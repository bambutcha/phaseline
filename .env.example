# API
PORT=8080
DATABASE_URL=postgres://phaseline:phaseline@localhost:5432/phaseline?sslmode=disable
LOG_LEVEL=info

# Web (browser talks to same origin via Caddy; leave empty in Docker)
NEXT_PUBLIC_API_BASE=

# Local without Caddy (Next on :3000, API on :8080)
# NEXT_PUBLIC_API_BASE=http://localhost:8080
